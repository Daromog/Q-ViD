datasets:
  tvqa: # name of the dataset builder
    # data_dir: ${env.data_dir}/datasets
    data_type: videos # [images|videos|features]
    build_info:
      # Be careful not to append minus sign (-) before split to avoid itemizing
      annotations:
        train:
          url: Datasets/TVQA/train.json
          storage: Datasets/TVQA/train.json
        val:
          url: Datasets/TVQA/val.json
          storage: Datasets/TVQA/val.json
        test:
          url: Datasets/TVQA/val.json
          storage: Datasets/TVQA/val.json
      videos:
        storage: Datasets/TVQA/videos