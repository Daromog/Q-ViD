datasets:
  intentqa: # name of the dataset builder
    # data_dir: ${env.data_dir}/datasets
    data_type: videos # [images|videos|features]
    build_info:
      # Be careful not to append minus sign (-) before split to avoid itemizing
      annotations:
        train:
          url: Datasets/Intent_QA/train.json
          storage: Datasets/Intent_QA/train.json
        val:
          url: Datasets/Intent_QA/val.json
          storage: Datasets/Intent_QA/val.json
        test:
          url: Datasets/Intent_QA/val.json
          storage: Datasets/Intent_QA/val.json
      videos:
        storage: Datasets/Intent_QA/fixed_videos